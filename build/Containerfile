FROM golang:1.23.4 AS build
WORKDIR /build

RUN useradd -u 10001 app-user

COPY go.mod go.sum .
RUN go mod download

FROM scratch AS app
WORKDIR /app

COPY --from=build /etc/passwd /etc/passwd

USER app-user
